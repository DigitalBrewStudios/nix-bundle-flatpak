# nix-bundle-flatpak
A Bundler for flatpak.

# Development/Testing

1. Have [nix installed](https://nixos.org/download) with experimential features `nix-command` & `flakes` enabled!
2. Do `nix bundle --bundler . <drv>` while on a linux system.
